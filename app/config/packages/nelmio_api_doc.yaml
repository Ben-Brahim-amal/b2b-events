nelmio_api_doc:
    documentation:
        info:
            title: B2B Events API
            description: API REST securisee pour la plateforme B2B Evenements d'Affaires
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api
        host_patterns: []